import { createSlice } from '@reduxjs/toolkit'


const <%= name %>Slice = createSlice({
    name: "<%= name %>",
    initialState: {
        <%= name %>List: []
    },
    reducers: {
        get<%= name %>List(state, { payload }) {
            state.<%= name %>List = payload
        }
    }
})

export const { get<%= name %>List } = <%= name %>Slice.actions
export default <%= name %>Slice.reducer